# Core data handling
pandas>=2.0
numpy>=1.24

# NLP tokenization
spacy>=3.7
# after pip install, also run: python -m spacy download en_core_web_sm

# bigram detection
gensim>=4.3

# language identification
fasttext-wheel>=0.9.2
# also requires the lid.176.bin model file (not pip-installable, download separately:
# https://fasttext.cc/docs/en/language-identification.html)

# modeling
scikit-learn>=1.3

# plotting
matplotlib>=3.7
seaborn>=0.13

# progress bars (build_parquets)
tqdm>=4.66

# parallel scoring (EDA notebook)
joblib>=1.3
